C：

#include<stdio.h>

int main()
{
  int n,step=0;     // n为输入的整数，step为步数
  
  scanf("%d",&n);   // 输入n
  while(n>1)
  {
        if(n%2==0)
          n/=2;
        else
          n = (3*n+1)/2;
        step++;
  }
  printf("%d\n",step);  // 输出步数
  
  return 0;
}

